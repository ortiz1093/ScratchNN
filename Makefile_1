FILENAMES = activation
EXE_NAME = BasicNN

output: $(FILENAMES).o main.o
	g++ main.o hello.o -o HELLO

main.o: main.cpp
	g++ -c main.cpp

$(FILENAMES).o: $(FILENAMES).cpp $(FILENAMES).h
	g++ -c $(FILENAMES).cpp

clean:
	rm *.o $(EXE_NAME).exe
